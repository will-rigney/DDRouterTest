# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DDRouterTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DDRouterTest
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'DDRouter', :path => '../github/ddrouter'
  pod 'DDMockiOS', :path => '../../ddmock'
  pod 'SwiftGen'
  # 'https://github.com/will-rigney/ddmock-ios', :branch => 'v2.0'

  target 'DDRouterTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
